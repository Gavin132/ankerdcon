/** Shared Tailwind class strings for admin drawer forms. */

export const F =
  "w-full rounded-xl border border-white/[0.12] bg-white/[0.06] px-3 py-2.5 text-sm text-white placeholder:text-slate-500 focus:outline-none focus:ring-2 focus:ring-sky-500/50 focus:border-sky-500/50 transition-colors [color-scheme:dark]";

/** Use for <select> elements — solid dark background so native dropdown options are readable. */
export const FS =
  "w-full rounded-xl border border-white/[0.12] bg-slate-900 px-3 py-2.5 text-sm text-white focus:outline-none focus:ring-2 focus:ring-sky-500/50 focus:border-sky-500/50 transition-colors [color-scheme:dark]";

export const L = "block text-xs text-slate-400 mb-1";

/** Uppercase section title inside a SECTION card. */
export const SECTION_TITLE =
  "text-[10px] font-semibold uppercase tracking-widest text-slate-500";

export const SECTION =
  "space-y-3 rounded-xl border border-white/[0.07] bg-white/[0.03] p-4";
